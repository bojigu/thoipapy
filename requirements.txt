# Exact pins for a reproducible install.
#
# This file mirrors the `dependencies` list in pyproject.toml and adds the transitive packages.
# pyproject.toml is what `pip install .` reads; this file exists because it is where most people
# look first, and because it records the full resolved environment rather than just the direct
# dependencies. If you change a version here, change it in pyproject.toml too.
#
# Everything is pinned with `==` on purpose. THOIPA is scientific software whose output has to
# stay reproducible for years. A `>=` range resolves to something different every time it is
# installed, and the result is a silently different prediction rather than an installation error.
#
# Verified together on Python 3.13.15. numpy and scipy require Python >= 3.12.
#
#     pip install -r requirements.txt
#
# Conda users should prefer environment.yml, which also installs the external command-line tools
# (BLAST, freecontact, cd-hit, rate4site) that pip cannot provide.

# --- direct dependencies (mirrors pyproject.toml) ---
biopython==1.88
joblib==1.5.3
matplotlib==3.11.1
numpy==2.5.2
openpyxl==3.1.5
pandas==3.0.5
psutil==7.2.2
pytoxr==0.0.7
scikit-learn==1.9.0
scipy==1.18.0
seaborn==0.13.2
weighslide==0.2.5

# --- transitive dependencies, pinned so the resolved environment is fully determined ---
contourpy==1.3.3
cycler==0.12.1
eccpy==0.5.0
et-xmlfile==2.0.0
fonttools==4.63.0
kiwisolver==1.5.0
narwhals==2.24.0
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
threadpoolctl==3.6.0
xlrd==2.0.2

# --- test only ---
# pytest==9.1.1
# iniconfig==2.3.0
# pluggy==1.6.0
# pygments==2.21.0
